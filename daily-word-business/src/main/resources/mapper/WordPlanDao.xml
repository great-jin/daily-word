<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="xyz.ibudai.dailyword.dao.WordPlanDao">

    <resultMap type="xyz.ibudai.dailyword.entity.WordPlan" id="WordPlanMap">
        <result property="id" column="id" jdbcType="VARCHAR"/>
        <result property="userName" column="user_name" jdbcType="VARCHAR"/>
        <result property="catalogue" column="catalogue" jdbcType="VARCHAR"/>
        <result property="totalSize" column="total_size" jdbcType="INTEGER"/>
        <result property="batchSize" column="batch_size" jdbcType="INTEGER"/>
        <result property="offset" column="offset" jdbcType="INTEGER"/>
        <result property="finished" column="finished" jdbcType="INTEGER"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into daily_word.word_plan(user_name, catalogue, total_size, batchSize, offset, finished)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userName}, #{entity.catalogue}, #{entity.totalSize}, #{entity.batchsize}, #{entity.offset},
            #{entity.finished})
        </foreach>
    </insert>

    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into daily_word.word_plan(user_name, catalogue, total_size, batchSize, offset, finished)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.userName}, #{entity.catalogue}, #{entity.totalSize}, #{entity.batchsize}, #{entity.offset},
            #{entity.finished})
        </foreach>
        on duplicate key update
        user_name = values(user_name) , catalogue = values(catalogue) , total_size = values(total_size) , batchSize =
        values(batchSize) , offset = values(offset) , finished = values(finished)
    </insert>
</mapper>

